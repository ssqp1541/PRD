# FR2: 윤리 영향 리포트 최소 단위 구현 시나리오

> TDD Green 단계를 위한 FR2 최소 구현 계획

**작성일**: 2025-12-15  
**목표**: FR2 테스트를 통과하기 위한 최소한의 구현  
**방법론**: TDD (Test-Driven Development) - Green 단계

---

## 📋 목차

1. [시나리오 개요](#시나리오-개요)
2. [테스트 요구사항 분석](#테스트-요구사항-분석)
3. [백엔드 구현 계획](#백엔드-구현-계획)
4. [프론트엔드 구현 계획](#프론트엔드-구현-계획)
5. [데이터베이스 연동](#데이터베이스-연동)
6. [탄소 발자국 계산 로직](#탄소-발자국-계산-로직)
7. [승인 요청](#승인-요청)

---

## 시나리오 개요

### 목표

FR2 테스트를 통과하기 위한 최소한의 구현을 완료합니다.

### 사용자 스토리

```
Given 고객이 3개월간 친환경 원두만 구매한 기록이 있을 때
When 고객이 마이페이지의 '나의 윤리적 영향' 섹션을 클릭하면
Then '누적 커피 탄소 발자국 절감량' 수치와 함께, 긍정적인 메시지가 표시되어야 한다.
```

### 구현 원칙

1. **테스트 통과 우선**: 작성된 테스트를 통과시키는 최소한의 코드만 구현
2. **데이터베이스 활용**: 이미 생성된 `purchases`, `products` 테이블 사용
3. **최소 기능**: 기본적인 계산 및 표시만 (고급 차트는 향후)
4. **에러 처리**: 404 에러 및 빈 데이터 처리 포함

### 구현 범위

#### ✅ 포함 사항
- 백엔드 API 엔드포인트 (`GET /api/users/:userId/ethical-impact`)
- EthicalImpactController 구현
- 탄소 발자국 계산 서비스
- 3개월간 구매 기록 집계
- 친환경 원두 필터링
- 긍정적인 메시지 생성
- 월별 집계 데이터 생성
- 프론트엔드 EthicalImpact 컴포넌트
- API 호출 함수 (`userApi.js`)
- 탄소 발자국 수치 표시
- 긍정적인 메시지 표시
- 월별 집계 표시 (기본 형식)
- 로딩 및 에러 상태 처리

#### ⚪ 제외 사항 (향후 구현)
- 고급 차트 라이브러리 (Chart.js, Recharts 등)
- 애니메이션 효과
- 상세 통계 분석
- 비교 기능 (다른 사용자와 비교)

---

## 테스트 요구사항 분석

### 백엔드 테스트 요구사항 (9개)

1. **200 상태 코드 반환**
   - `GET /api/users/:userId/ethical-impact` 엔드포인트

2. **탄소 발자국 절감량 반환**
   - `carbonFootprintReduction` 속성
   - 숫자 타입
   - 0 이상

3. **3개월간 구매 기록 집계**
   - `purchasePeriod` 객체
   - `startDate`, `endDate` 속성
   - 약 3개월 기간 (오차 허용)

4. **친환경 원두 필터링**
   - `ecoFriendlyPurchases` 배열
   - 모든 항목이 `isEcoFriendly: true`

5. **긍정적인 메시지 반환**
   - `message` 속성
   - 긍정적인 키워드 포함 ('감사', '환경', '기여', '절감', '좋은', '긍정')

6. **404 에러 처리**
   - 존재하지 않는 사용자 ID에 대해 404 반환

7. **빈 데이터 처리**
   - 구매 기록이 없을 때 `carbonFootprintReduction: 0`, 빈 배열 반환

8. **계산 정보 반환**
   - `calculation` 객체
   - `totalPurchases`, `totalKg`, `averageReductionPerKg` 속성

9. **월별 집계 반환**
   - `monthlyBreakdown` 배열
   - 최대 3개월 데이터

### 프론트엔드 테스트 요구사항 (8개)

1. **섹션 렌더링**
   - `data-testid="ethical-impact-section"` 요소

2. **탄소 발자국 절감량 표시**
   - "탄소 발자국 절감량" 텍스트
   - 숫자 및 "kg" 단위 표시

3. **긍정적인 메시지 표시**
   - `data-testid="impact-message"` 요소
   - "감사" 키워드 포함

4. **구매 기간 정보 표시**
   - "3개월간" 텍스트

5. **로딩 상태 처리**
   - `data-testid="loading-spinner"` 요소

6. **에러 상태 처리**
   - `data-testid="error-message"` 요소

7. **월별 집계 표시**
   - `data-testid="monthly-breakdown"` 요소

8. **빈 상태 처리**
   - "구매 기록이 없습니다" 텍스트

---

## 백엔드 구현 계획

### 1. 프로젝트 구조

```
backend/src/
├── modules/
│   └── tracking/
│       ├── ethicalImpactController.js    # 컨트롤러 (신규)
│       └── ethicalImpactRoutes.js       # 라우트 (신규)
├── services/
│   └── carbon/
│       └── carbonService.js             # 탄소 발자국 계산 서비스 (신규)
└── models/
    └── Purchase.js                       # Purchase 모델 (신규)
```

### 2. 구현 파일 상세

#### 2.1 Purchase 모델 (`models/Purchase.js`)

**기능**:
- 데이터베이스에서 구매 기록 조회
- 3개월간 구매 기록 조회
- 친환경 원두 필터링

**메서드**:
```javascript
- findByUserIdAndDateRange(userId, startDate, endDate)
- findEcoFriendlyByUserIdAndDateRange(userId, startDate, endDate)
```

#### 2.2 Carbon Service (`services/carbon/carbonService.js`)

**기능**:
- 탄소 발자국 절감량 계산
- 월별 집계 계산
- 평균 절감량 계산

**함수**:
```javascript
- calculateCarbonReduction(purchases)
- calculateMonthlyBreakdown(purchases)
- generatePositiveMessage(carbonReduction, totalPurchases)
```

**계산 로직**:
- 친환경 원두 1kg당 평균 탄소 절감량: 12.5kg CO2
- 총 절감량 = 구매량(kg) × 12.5

#### 2.3 EthicalImpactController (`modules/tracking/ethicalImpactController.js`)

**기능**:
- HTTP 요청/응답 처리
- 에러 처리

**엔드포인트 핸들러**:
```javascript
- getEthicalImpact(req, res, next)
```

#### 2.4 EthicalImpactRoutes (`modules/tracking/ethicalImpactRoutes.js`)

**라우트 정의**:
```javascript
GET /api/users/:userId/ethical-impact
```

### 3. API 응답 형식

**성공 응답** (200 OK):
```json
{
  "success": true,
  "carbonFootprintReduction": 12.5,
  "purchasePeriod": {
    "startDate": "2025-09-15T00:00:00.000Z",
    "endDate": "2025-12-15T00:00:00.000Z"
  },
  "ecoFriendlyPurchases": [
    {
      "id": 1,
      "productName": "에티오피아 예가체프 친환경 원두",
      "isEcoFriendly": true,
      "quantity": 0.5,
      "purchaseDate": "2025-10-15T00:00:00.000Z"
    }
  ],
  "message": "3개월간 친환경 원두 구매로 12.5kg의 탄소를 절감했습니다. 환경 보호에 기여해주셔서 감사합니다!",
  "calculation": {
    "totalPurchases": 2,
    "totalKg": 1.0,
    "averageReductionPerKg": 12.5
  },
  "monthlyBreakdown": [
    {
      "month": "2025-10",
      "reduction": 4.2
    },
    {
      "month": "2025-11",
      "reduction": 4.1
    },
    {
      "month": "2025-12",
      "reduction": 4.2
    }
  ]
}
```

**에러 응답** (404 Not Found):
```json
{
  "success": false,
  "message": "사용자를 찾을 수 없습니다.",
  "error": "USER_NOT_FOUND"
}
```

---

## 프론트엔드 구현 계획

### 1. 프로젝트 구조

```
frontend/src/
├── pages/
│   └── MyPage/
│       └── EthicalImpact.jsx            # 컴포넌트 (개선)
└── services/
    └── api/
        └── userApi.js                   # API 클라이언트 (개선)
```

### 2. 구현 파일 상세

#### 2.1 EthicalImpact 컴포넌트 (`pages/MyPage/EthicalImpact.jsx`)

**기능**:
- 윤리 영향 정보 표시
- 탄소 발자국 절감량 표시
- 긍정적인 메시지 표시
- 월별 집계 표시
- 로딩 및 에러 상태 처리

**UI 요소**:
- `data-testid="ethical-impact-section"` 섹션
- 탄소 발자국 절감량 (숫자 + kg)
- 긍정적인 메시지 (`data-testid="impact-message"`)
- 구매 기간 정보
- 월별 집계 (`data-testid="monthly-breakdown"`)
- 로딩 스피너 (`data-testid="loading-spinner"`)
- 에러 메시지 (`data-testid="error-message"`)
- 빈 상태 메시지

**상태 관리**:
```javascript
- isLoading: boolean
- impactData: object | null
- error: string | null
```

#### 2.2 userApi (`services/api/userApi.js`)

**기능**:
- 백엔드 API 호출
- 에러 처리

**함수**:
```javascript
- getEthicalImpact(userId)
```

---

## 데이터베이스 연동

### 사용할 테이블

**purchases 테이블** (이미 생성됨):
```sql
- id
- user_id (FK -> users.id)
- product_id (FK -> products.id)
- quantity (구매 수량, kg)
- purchase_date (구매 일시)
```

**products 테이블** (이미 생성됨):
```sql
- id
- name
- is_eco_friendly (BOOLEAN)
```

### 쿼리 예시

```sql
-- 3개월간 친환경 원두 구매 기록 조회
SELECT 
  p.id,
  p.quantity,
  p.purchase_date,
  pr.name as product_name,
  pr.is_eco_friendly
FROM purchases p
INNER JOIN products pr ON p.product_id = pr.id
WHERE p.user_id = $1
  AND p.purchase_date >= $2
  AND p.purchase_date <= $3
  AND pr.is_eco_friendly = true
ORDER BY p.purchase_date DESC;
```

---

## 탄소 발자국 계산 로직

### 계산 공식

1. **기본 절감량 계산**:
   - 친환경 원두 1kg당 평균 탄소 절감량: **12.5kg CO2**
   - 총 절감량 = Σ(구매량(kg) × 12.5)

2. **월별 집계**:
   - 각 월별 구매량 합계
   - 월별 절감량 = 월별 구매량 × 12.5

3. **평균 계산**:
   - 평균 절감량/kg = 총 절감량 / 총 구매량(kg)

### 긍정적인 메시지 생성

**메시지 템플릿**:
- 구매 기록이 있을 때: "3개월간 친환경 원두 구매로 {절감량}kg의 탄소를 절감했습니다. 환경 보호에 기여해주셔서 감사합니다!"
- 구매 기록이 없을 때: "아직 구매 기록이 없습니다. 친환경 원두를 구매하여 환경 보호에 기여해보세요!"

---

## 구현 파일 목록

### 백엔드 파일 (5개)

1. `backend/src/models/Purchase.js` - Purchase 모델 (신규)
2. `backend/src/services/carbon/carbonService.js` - 탄소 계산 서비스 (신규)
3. `backend/src/modules/tracking/ethicalImpactController.js` - 컨트롤러 (신규)
4. `backend/src/modules/tracking/ethicalImpactRoutes.js` - 라우트 (신규)
5. `backend/src/routes/index.js` - 라우트 통합 (업데이트)

### 프론트엔드 파일 (2개)

1. `frontend/src/pages/MyPage/EthicalImpact.jsx` - 컴포넌트 (개선)
2. `frontend/src/services/api/userApi.js` - API 클라이언트 (개선)
3. `frontend/src/config/api.js` - API 설정 (업데이트)

---

## 예상 결과

### 생성될 파일
- **백엔드**: 5개 파일 (신규 4개, 업데이트 1개)
- **프론트엔드**: 3개 파일 (개선 2개, 업데이트 1개)
- **총 8개 파일**

### 기능
- ✅ 사용자별 윤리 영향 리포트 조회 API
- ✅ 탄소 발자국 절감량 계산
- ✅ 3개월간 구매 기록 집계
- ✅ 친환경 원두 필터링
- ✅ 긍정적인 메시지 생성
- ✅ 월별 집계 데이터 생성
- ✅ 탄소 발자국 수치 표시
- ✅ 긍정적인 메시지 표시
- ✅ 월별 집계 표시 (기본 형식)
- ✅ 로딩 및 에러 상태 처리

---

## 승인 요청

### 구현 계획 요약

1. **백엔드 구현** (5개 파일)
   - Purchase 모델 생성
   - Carbon Service 구현 (탄소 발자국 계산)
   - EthicalImpactController 구현
   - EthicalImpactRoutes 생성
   - 라우트 통합 파일 업데이트

2. **프론트엔드 구현** (3개 파일)
   - EthicalImpact 컴포넌트 구현
   - userApi 함수 구현
   - API 설정 업데이트

3. **테스트 통과 확인**
   - 백엔드 테스트 9개 케이스 통과
   - 프론트엔드 테스트 8개 케이스 통과

### 예상 소요 시간

- 백엔드 구현: 3-4시간
- 프론트엔드 구현: 2-3시간
- 테스트 및 디버깅: 1-2시간
- **총 6-9시간** (1-1.5일)

### 승인 여부

다음 작업을 진행하시겠습니까?

- [ ] ✅ **승인** - FR2 윤리 영향 리포트 구현 시작
- [ ] ⏸️ **보류** - 추가 검토 필요
- [ ] ✏️ **수정 요청** - 시나리오 변경 필요

**승인 시 진행할 작업**:
1. 백엔드 Purchase 모델 및 Carbon Service 구현
2. 백엔드 EthicalImpactController 및 Routes 구현
3. 프론트엔드 EthicalImpact 컴포넌트 구현
4. 프론트엔드 userApi 함수 구현
5. 테스트 실행 및 통과 확인

---

**작성일**: 2025-12-15  
**승인 대기 중**

