/**
 * 브레이크포인트 상수
 * 반응형 디자인에서 사용하는 화면 크기 기준값
 */

export const BREAKPOINTS = {
  MOBILE: 768,
  TABLET: 1024,
  DESKTOP: 1200,
};

/**
 * 기본 모바일 브레이크포인트 (가장 많이 사용되는 값)
 */
export const DEFAULT_MOBILE_BREAKPOINT = BREAKPOINTS.MOBILE;

